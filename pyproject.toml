[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "canonicalgs"
version = "0.1.0"
description = "CanonicalGS gs_cube project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["canonicalgs*"]
